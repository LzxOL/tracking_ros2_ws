# TrackOn ROS2 配置文件
# 用于配置跟踪相关的参数

# 机器人配置
robot:
  ip: "192.168.11.3:50051"
  component_type: 1  # 1: 左臂/左相机, 2: 右臂/右相机
  init_robot_arm: true  # 是否初始化机器人手臂

# 左相机配置
left_camera:
  camera_topic: "/left/color/video"
  depth_topic: "/left/depth/stream"
  intrinsics_file: "config/camera_lhand_front_intrinsics_02_9.txt"

# 右相机配置
right_camera:
  camera_topic: "/right/color/video"
  depth_topic: "/right/depth/stream"
  intrinsics_file: "config/camera_rhand_front_intrinsics_02_9.txt"

# 跟踪参数
tracking:
  checkpoint_path: "src/track_on/checkpoints/track_on_checkpoint.pt"
  publish_visualization: true
  show_interactive_window: true
